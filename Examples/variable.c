#include<stdio.h>

int main(){

   int numbers = 100; // declaring a variable named 'numbers' and assigning

   printf("%d\n",numbers); // printing the value of numbers using %d format specifier
   
   numbers = 900; // reassigning the value to numbers
   printf("%d\n",numbers);

   return 0; // returning 0 indicates successful execution of program


   
}