#include <stdio.h>
#include <string.h>

int main() {
    char str[] = "codeswithpankaj.com";
    int length = strlen(str); // length is 19
    printf("Length of the string: %d\n", length);
    return 0;
}